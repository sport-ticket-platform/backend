info:
  name: login with otp email
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/login-otp-email
  body:
    type: json
    data: |-
      {
        "email": "taha1@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
