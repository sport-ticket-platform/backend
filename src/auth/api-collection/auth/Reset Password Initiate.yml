info:
  name: Reset Password Initiate
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:8080/api/auth/reset-password/initiate
  body:
    type: json
    data: |-
      {
        "email": "taha1@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
