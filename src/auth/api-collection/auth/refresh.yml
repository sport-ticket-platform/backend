info:
  name: refresh
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8080/api/auth/refresh
  body:
    type: json
    data: |-
      {
        "refresh_token": "d91dc0c9-4473-482c-a122-7dd61512ab28"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
