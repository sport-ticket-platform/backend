info:
  name: Signup Complete
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:8080/api/auth/signup/complete
  body:
    type: json
    data: |-
      {

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
