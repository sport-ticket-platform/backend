info:
  name: login with otp phone
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/login-otp-phone
  body:
    type: json
    data: |-
      {
        "phone": "9123456789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
