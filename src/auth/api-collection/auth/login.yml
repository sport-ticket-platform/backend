info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/login
  headers:
    - name: X-Device-Id
      value: SOME_TEST_VALUE
    - name: User-Agent
      value: Testing by bruno
  body:
    type: json
    data: |-
      {
        "username": "taha",
        "password": "taha"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
