info:
  name: login with password
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/auth/login-password
  headers:
    - name: X-Device-Id
      value: SOME_TEST_VALUE
    - name: User-Agent
      value: Testing by bruno
  body:
    type: json
    data: |-
      {
        "identifier": "taha2@gmail.com",
        "password": "taha"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
